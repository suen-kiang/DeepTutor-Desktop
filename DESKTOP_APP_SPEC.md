# DeepTutor macOS 桌面应用封装 — 开发计划 Spec

> 版本：v1.0　|　日期：2026-08-18　|　适用系统：macOS 15.7.9  
> 读者定位：无技术背景的初学者（术语均附带白话说明）

---

## 1. 背景与目标

DeepTutor 是一个开源的"智能体学习工作台"，官方提供三种使用方式：命令行（CLI）、网页（Web UI）、Docker 容器。本项目的目标是**在官方能力之上，再包一层 macOS 桌面应用**，让用户像用普通 Mac 软件一样：双击图标打开、图标常驻菜单栏（系统托盘）、一键启动/停止服务、打包成 `.app` 应用分发（分发时将 `.app` 压缩成 `.zip` 上传）。

**一句话总结**：官方项目是"引擎"，我们做的是一个"外壳（车壳）"。引擎不动，只给它配一个好看、好用的车壳。

### 必须遵守的三条铁律

1. **核心能力零改动**：智能体、插件系统、Web UI 等官方功能，一行代码都不改。
2. **最小改动**：禁止大型重构，能"包一层"解决的就绝不改内部代码。
3. **跟得上官方更新**：官方后续发新版本，桌面层必须能继续正常工作，不被"官方一更新就坏掉"。

---

## 2. 现状分析（遍历结论）

### 2.1 技术栈

| 层次  | 技术                                 | 白话说明                      |
| --- | ---------------------------------- | ------------------------- |
| 后端  | Python 3.11–3.13 + FastAPI         | 负责"思考"和"干活"的服务，跑在 8001 端口 |
| 前端  | Next.js 16 + React 19 + TypeScript | 负责"界面展示"的网页，跑在 3782 端口    |
| 命令行 | Typer（`deeptutor` 命令）              | 在终端里输入命令操作软件              |
| 存储  | 本地 JSON/YAML 文件                    | 配置和数据都存在 `data/` 文件夹里     |

### 2.2 启动方式

官方提供一条核心命令，一条命令同时拉起后端和前端：

```bash
deeptutor start          # 生产模式：同时启动后端(8001) + 前端(3782)
deeptutor start --dev    # 开发模式：前端带热重载
deeptutor serve          # 只启动后端 API
deeptutor init           # 首次使用前的交互式配置向导
```

### 2.3 关键入口文件（桌面层必须对接的）

| 文件                              | 作用                                                   |
| ------------------------------- | ---------------------------------------------------- |
| `deeptutor_cli/main.py`         | CLI 命令入口，定义了 `start` / `serve` / `init` 等命令          |
| `deeptutor/runtime/launcher.py` | **核心启动器**，`start()` 函数统一编排"启动后端+前端、处理端口冲突、健康检查、优雅退出" |
| `deeptutor/api/main.py`         | 后端 FastAPI 应用入口                                      |
| `web/proxy.ts`                  | 前端中间件，把 `/api/*`、`/ws/*` 请求转发给后端                     |
| `deeptutor/runtime/home.py`     | 解析数据目录位置（`DEEPTUTOR_HOME` 环境变量）                      |

### 2.4 三个"稳定接口"（隔离的关键，务必记住）

桌面层与官方源码之间，**只依赖下面三样东西**，这三样是官方长期稳定的公开接口：

1. **命令**：`deeptutor start` —— 启动服务的入口。
2. **环境变量**：`DEEPTUTOR_HOME` —— 指定"数据存哪个文件夹"，不设则默认当前目录。
3. **端口**：后端 `8001`、前端 `3782` —— 桌面窗口访问前端页面的地址。

**核心结论**：官方 `launcher.py` 的 `start()` 已经完整实现了"进程编排"（启动、健康检查、端口冲突处理、收到信号后优雅退出）。**桌面层不需要重写这些逻辑，只需把 `deeptutor start` 当作一个子进程跑起来即可。**

---

## 3. 核心设计：隔离方案

### 3.1 隔离原则：进程隔离 + 接口隔离

桌面层和官方源码之间，用两层"防火墙"隔开：

- **进程隔离**：桌面层通过**子进程**（subprocess）运行 `deeptutor start`，官方代码跑在独立进程里，两者互不侵入。
- **接口隔离**：桌面层只调用上面 2.4 节的三个稳定接口，**不 import（引用）任何官方 Python 代码**。官方内部怎么改，只要三个接口不变，桌面层就无感知。

### 3.2 目录结构（唯一新增：`desktop/`）

```
DeepTutor/                      # 官方仓库（保持不动）
├── deeptutor/                  # 官方后端（不动）
├── deeptutor_cli/              # 官方 CLI（不动）
├── web/                        # 官方前端（不动）
├── ...                         # 其他官方文件（不动）
└── desktop/                    # ★ 唯一新增目录：桌面封装层
    ├── package.json            # 桌面层自己的项目配置
    ├── src/
    │   ├── main.ts             # 主进程：窗口 + 托盘 + 生命周期
    │   ├── service-manager.ts  # 服务管理：调用 deeptutor start / 停止 / 健康检查
    │   ├── tray.ts             # 系统托盘菜单
    │   └── window.ts           # 桌面窗口创建
    ├── assets/                 # 图标、启动图等
    └── build/                  # 打包配置（生成 .app）
```

### 3.3 为什么这样能"跟得上官方更新"

| 官方可能的更新                             | 桌面层是否受影响               |
| ----------------------------------- | ---------------------- |
| 改智能体引擎内部实现                          | 不受影响（进程隔离，无引用）         |
| 改 Web UI 界面                         | 不受影响（窗口只是加载网页，网页变啥显示啥） |
| 改后端接口细节                             | 不受影响（走前端代理，桌面层不直接调后端）  |
| 改 `deeptutor start` 命令名 / 端口 / 环境变量 | **需小改**（概率极低，属官方公开接口）  |

桌面层对官方代码的依赖被压到"三个稳定接口"这一个最小集合，因此官方 99% 的更新都不会波及桌面层。

---

## 4. 桌面封装层技术选型

### 4.1 候选方案对比

| 维度         | Electron                       | Tauri         |
| ---------- | ------------------------------ | ------------- |
| 语言         | JavaScript/TypeScript（与官方前端一致） | Rust + Web 前端 |
| 学习门槛       | 低（你会 JS 就能上手）                  | 高（需先学 Rust）   |
| 安装包体积      | 大（约 150MB+）                    | 小（约 10MB）     |
| 内存占用       | 高（约 100–300MB）                 | 低             |
| 打 .app 成熟度 | 非常成熟（electron-builder）         | 成熟            |
| 托盘/窗口 API  | 丰富、文档多                         | 丰富            |
| 与官方工具链契合   | 高（官方前端就是 Node 生态）              | 中             |

### 4.2 推荐结论（已确认）

**技术选型已确认：Electron，封装为 `.app`。**

1. 官方前端本身就是 Node.js/TypeScript 生态，桌面层用 Electron 与之一脉相承，工具链统一。
2. 用户是无技术背景的初学者，Electron 全 JS/TS、资料最多、报错最好搜。
3. `electron-builder` 打 `.app` 成熟稳定。
4. 本场景桌面层只是"壳"（启动服务 + 加载网页 + 托盘），Electron 的"体积大"缺点可接受。

### 4.3 运行环境策略（已确认）

**结论：依赖本机环境（瘦包），不把 Python/Node 运行时打包进 .app。**

| 运行时 | 方案 | 版本 |
|--------|------|------|
| Python | 使用 brew 的 `python@3.12` 建 venv，路径写死在桌面层配置 | 3.12.14（满足官方 3.11–3.14） |
| Node/npm | 运行时依赖本机；前端编译产物（Next.js standalone）在打包阶段**预先生成**，随 .app 分发 | 本机任意版本 |

**关键机制**：源码模式下 `deeptutor start` 会现场 `npm run build`，对 Node 版本敏感。为避免"Node 随时更新导致前端编译失败"，打包阶段预先编译好前端 standalone 产物，运行时只走 `node server.js`，不再现场编译，从而降低对 Node 版本漂移的敏感度。

> Tauri 为备选方案，若日后追求极致体积可再迁移。



---

## 5. 功能设计

### 5.1 本地服务启动 / 停止 / 恢复（核心）

桌面层内置一个"服务管家"（service-manager），职责如下：

- **启动**：用子进程执行 `deeptutor start --home <桌面专属数据目录>`，并设置 `DEEPTUTOR_HOME` 指向桌面应用自己的数据文件夹（如 `~/Library/Application Support/DeepTutor`），避免和用户命令行数据混在一起。
- **停止**：向该子进程发送终止信号（SIGTERM）。官方 `launcher.py` 已注册信号处理，会优雅关闭前端和后端。
- **恢复 / 状态探测**：定时探测 `http://127.0.0.1:3782` 和 `http://127.0.0.1:8001` 是否响应：
  - 都通 → 显示"运行中"；
  - 进程没了 → 显示"已停止"；
  - 进程在但端口不通 → 判定"异常"，按策略自动重启。

> 白话说明：服务管家就像"汽车点火按钮"，负责帮你把官方引擎启动起来、关闭掉、以及发现熄火了自动重新点火。

### 5.2 桌面窗口

- 启动服务成功后，创建一个桌面窗口，加载 `http://localhost:3782`（官方前端页面）。
- 关闭窗口时**不退出应用**，而是收进托盘（最小化到菜单栏），保持服务后台运行。
- 可选优化（不影响官方代码，只在壳层做）：
  - 无边框或自定义标题栏，更像原生 App；
  - 窗口尺寸记忆（记住上次的大小和位置）。

### 5.3 系统托盘

菜单栏图标（托盘）右键菜单包含：

- 打开主窗口
- 启动 / 停止服务
- **终端**：打开一个系统终端并连接进 DeepTutor（激活项目 venv 后进入 `deeptutor` 命令行环境，可执行 `deeptutor chat` 等 CLI 命令）
- 退出应用（退出时自动停止服务）

### 5.4 打包发布（.app + zip）

用 `electron-builder` 生成 `.app`，分发时将 `.app` 压缩成 `.zip` 上传：

- `.app`：macOS 应用本体（双击即可运行）。
- 分发方式：把 `.app` 压缩成 `.zip` 上传（如 GitHub Releases）。用户下载后解压，拖入"应用程序"即可。
- 不涉及代码签名与公证（notarization）：本项目为自用/源码分发，不配置 Apple 开发者证书。未签名版本在本地可正常使用，仅首次打开时系统可能提示"来自未识别的开发者"，需在「系统设置 → 隐私与安全性」里允许打开。

### 5.5 界面体验优化（仅壳层，不碰官方 Web UI）

- 原生窗口体验（无地址栏、无浏览器 UI）。
- 跟随系统深色/浅色模式。
- 首次启动引导（检测 Python/Node 环境是否就绪，缺啥提示装啥）。

---

## 6. 分阶段实施计划

### 阶段 0：环境准备

- 确认 Python 3.11–3.13、Node.js 22 已安装。
- 创建虚拟环境并安装官方依赖（`pip install -e .`）。
- **验收**：手动运行 `deeptutor start`，浏览器能打开 3782 端口页面。

### 阶段 1：最小可运行壳（MVP）

- 创建 `desktop/` Electron 项目骨架。
- 实现 service-manager：子进程启动 `deeptutor start`、停止、健康检查。
- 实现简单窗口加载 3782 页面。
- **验收**：双击运行，窗口能显示官方界面；关掉窗口服务能正常停止。

### 阶段 2：托盘 + 生命周期完善

- 实现托盘菜单（启动/停止/退出）。
- 窗口关闭收进托盘。
- 退出应用自动停服务。
- **验收**：托盘可控制服务启停，关闭窗口后软件仍在菜单栏运行。

### 阶段 3：打包发布

- 配置 electron-builder，生成 `.app`。
- 处理图标、应用名、版本号。
- 分发时将 `.app` 压缩成 `.zip` 上传。
- **验收**：解压后拖入"应用程序"，可正常打开使用。

### 阶段 4：体验优化（可选迭代）

- 无边框窗口、尺寸记忆、深色模式跟随、首次启动引导。

---

## 7. 风险与注意事项

1. **端口冲突**：8001/3782 被占用时，官方 `start` 会在非交互环境直接报错退出。桌面层需预先探测端口，冲突时提示用户或自动改端口。
2. **首次初始化**：首次使用可能需要 `deeptutor init` 配置模型。桌面层应在首启时检测配置是否就绪，未就绪则引导用户在网页里完成设置。
3. **Python/Node 依赖**：`.app` 分发给别人时，对方机器需有 Python 和 Node（或后续把运行环境一并打包进 `.app`，属进阶优化）。
4. **官方公开接口变更**：极小概率官方改动命令名或端口，桌面层把这三个接口的取值集中写在一处配置，便于快速适配。
5. **数据目录隔离**：务必通过 `DEEPTUTOR_HOME` 把桌面数据与命令行数据分开，避免互相干扰。

---

## 8. 当前状态

本 Spec 已完成，桌面封装层（`desktop/` 目录）已按此方案实现并验证通过，`.app` 已打包。后续可选的迭代方向：窗口体验优化（无边框、尺寸记忆）、运行环境打包进 `.app`（进阶）、GitHub 发布流程自动化。
